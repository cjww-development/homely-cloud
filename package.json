{
  "name": "hello_world",
  "version": "1.0.0",
  "description": "hello world sample for NodeJS",
  "repository": "https://github.com/cjww-development/homely-cloud",
  "author": "SAM CLI",
  "license": "MIT",
  "dependencies": {
    "@middy/core": "^1.0.0",
    "@middy/do-not-wait-for-empty-event-loop": "^1.0.0",
    "@middy/http-error-handler": "^1.0.0",
    "@middy/http-json-body-parser": "^1.0.0",
    "@middy/validator": "^1.0.0",
    "aws-sdk": "^2.689.0",
    "axios": "^0.18.0",
    "date-fns": "^2.14.0",
    "firebase-admin": "^9.0.0",
    "jsonwebtoken": "^8.5.1",
    "typescript": "^3.9.5",
    "uuid": "^8.3.0",
    "winston": "^3.2.1"
  },
  "scripts": {
    "start": "yarn build && sam local start-api -t template.yaml --skip-pull-image",
    "build": "tsc && cp package.json dist/package.json && cp ./service-account.json ./dist/service-account.json && cp ./database.json ./dist/database.json && cd dist && yarn install --prod",
    "test": "jest --verbose false --runInBand"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.55",
    "@types/jsonwebtoken": "^8.5.0",
    "@types/node": "^14.0.13",
    "@types/uuid": "^8.0.1",
    "chai": "^4.2.0",
    "jest": "^26.0.1",
    "mocha": "^6.1.4"
  }
}
